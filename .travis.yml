language: cpp

compiler:
  - gcc
  - clang

before_install:
  - chmod +x install-sdl-linux.sh
  - ./install-sdl-linux.sh

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make

# TODO: try make UT work on travis

